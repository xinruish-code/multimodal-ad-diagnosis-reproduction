# Preprocessing

This folder contains preprocessing scripts for MRI, PET, metadata, and CSV generation.

## Planned Structure

- `preprocess_mri.py`: preprocessing pipeline for structural MRI
- `preprocess_pet.py`: preprocessing pipeline for PET images
- `preprocess_metadata.py`: preprocessing and cleaning for metadata
- `make_csv.py`: generate train, validation, and test CSV files
