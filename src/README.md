# Source Code

This folder contains datasets, model implementations, loss functions, training scripts, and evaluation scripts.

## Planned Structure

- `datasets/`: dataset loading and data split scripts
- `models/`: reproduced model implementations
- `losses/`: custom loss functions
- `train.py`: training entry point
- `evaluate.py`: evaluation entry point
- `utils.py`: utility functions
